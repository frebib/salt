# Minion Salt image

## Usage

This image expects to see salt master in open mode at `salt-master` host.

In general, consult salt repo for the way to run dev containers:

* https://stash.cfops.it/projects/DEVOPS/repos/salt
